import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mapios',
  appName: 'mapFinalIosCapacitorProject',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
